Raw downloads land here when you run `python scripts/fetch_data.py` (or
`python run_pipeline.py`). This directory is intentionally not checked into
git -- see data_sources.md for exact download URLs.

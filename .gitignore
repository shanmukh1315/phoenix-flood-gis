# Python
__pycache__/
*.pyc
.ipynb_checkpoints/
venv/
.venv/
*.egg-info/

# OS
.DS_Store

# SQLite/GeoPackage temp lock files (created transiently when QGIS opens a .gpkg)
*.gpkg-shm
*.gpkg-wal
*.gpkg-journal

# Raw downloads (regenerated by scripts/fetch_data.py; large, and not
# needed to view the portfolio -- see data_sources.md to re-download them)
data/raw/*
!data/raw/.gitkeep

# Processed outputs, maps, and reports ARE committed (not ignored) so the
# portfolio is viewable on GitHub without re-running the pipeline.
